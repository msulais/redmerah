---
import { ThemeData } from '@/enums/theme'
import { generate_color, hex_to_rgb } from '@/utils/color'
import { CornerData } from '@/enums/corner'
import { ElementIds } from '@/enums/ids'
import type { HEXColor } from '@/types/color'
import { PlatformData } from '@/enums/platforms'
import '@/styles/variables.scss'
import '@/styles/animations.scss'
import '@/styles/index.scss'

type Props = {
	title?: string
	description?: string
	lang?: string
	theme?: ThemeData
	accent_color?: HEXColor
	corner?: CornerData
	component_count?: number
}

const {
	title = 'Redmerah - Increase your productivity with our apps',
	description = 'Get things done faster with zero interruptions! Redmerah offers powerful, privacy-focused tools to boost your productivity. Free, secure & data stays local.',
	lang = 'en-US',
	theme = ThemeData.system,
	accent_color = '#FF0000',
	corner = CornerData.round,
	component_count = 0
} = Astro.props

const colors = generate_color(accent_color)
const color_to_css = (hex: HEXColor) => {
	const rgb = hex_to_rgb(hex)
	return `${rgb.r}, ${rgb.g}, ${rgb.b}`
}
---

<html lang={ lang } data-platform={ PlatformData.desktop } data-theme={ theme } data-corner={ corner }>
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="theme-color" content={ accent_color } />
		<meta name="description" content={ description }>
		<title>{ title }</title>
		<style id={ElementIds.color_accent} set:html={`:root{--g-color-accent-light: ${color_to_css(colors.color)};--g-color-accent-dark: ${color_to_css(colors.color_dark)};--g-color-on-accent-light: ${color_to_css(colors.on_color)};--g-color-on-accent-dark: ${color_to_css(colors.on_color_dark)};}`}/>
		<slot name="favicon">
			<link rel="shortcut icon" type="image/ico" href="/favicon/favicon.ico" />
			<link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
			<link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
			<link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
			<link rel="manifest" href="/favicon/site.webmanifest">
		</slot>
		<slot name="open-graph-protocol" />
	</head>
	<body data-g-component-count-max={component_count}>
		<slot />
	</body>
</html>