---
import { attrClassList } from '@/utils/attributes'

import {
	MenuClasses,
	type MenuProps
} from '.'
import Popover from '@/native-components/Popover/index.astro'
import './index.scss'

type Props = MenuProps
const {
	'class': className,
	MenuContentAttr,
	MenuSubMenusAttr,
	PopoverGap = 8,
	PopoverPadding = 5, // +1px border
	role,
	...other
} = Astro.props
const {'class': contentClass, ...contentOther} = MenuContentAttr ?? {}
const {'class': submenusClass, ...submenusOther} = MenuSubMenusAttr ?? {}
---

<Popover
	role={role ?? 'menu'}
	class={attrClassList(MenuClasses.menu, className)}
	PopoverGap={PopoverGap}
	PopoverPadding={PopoverPadding}
	{...other}>
	<div class={attrClassList(MenuClasses.content, contentClass)} {...contentOther}>
		<slot />
	</div>
	<div class={attrClassList(MenuClasses.submenus, submenusClass)} {...submenusOther}>
		<slot name="submenus"/>
	</div>
</Popover>
<script>
	import { registerMenuRef } from '.'
	registerMenuRef()
</script>