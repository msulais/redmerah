---
import { attrClassList } from '@/utils/attributes'

import {
	type TextFieldProps,
	TextFieldClasses
} from '.'
import './index.scss'

type Props = TextFieldProps
const {
	'class': className,
	TextFieldInputAttr    = {},
	TextFieldLeadingAttr  = {},
	TextFieldTrailingAttr = {},
	...other
} = Astro.props
const {'class': inputClass   , type: inputType = "text", ...inputOther} = TextFieldInputAttr
const {'class': leadingClass , ...leadingOther } = TextFieldLeadingAttr
const {'class': trailingClass, ...trailingOther} = TextFieldTrailingAttr
---

<div class={attrClassList(TextFieldClasses.textfield, className)} {...other}>
	<div class={attrClassList(TextFieldClasses.leading, leadingClass)} {...leadingOther}>
		<slot name="leading"/>
	</div>
	<input
		type={inputType}
		class={attrClassList(TextFieldClasses.input, inputClass)}
		{...inputOther}
	/>
	<div class={attrClassList(TextFieldClasses.trailing, trailingClass)} {...trailingOther}>
		<slot name="trailing"/>
	</div>
</div>