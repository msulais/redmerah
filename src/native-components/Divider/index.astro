---
import { joinClassList } from '@/utils/attributes'

import {
	type DividerProps,
	DividerClasses
} from '.'
import './index.scss'

type Props = DividerProps
const {
	'class': className,
	...other
} = Astro.props
---

<div class={joinClassList(DividerClasses.divider, className)} {...other}/>