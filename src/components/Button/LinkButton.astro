---
import { setAttrIfExist } from '@/utils/attributes'

import { ButtonVariant, ButtonIndicatorPosition } from '.'
import './index.scss'

type Props = astroHTML.JSX.AnchorHTMLAttributes  & {
	variant?: ButtonVariant
	focus?: boolean
	compact?: boolean
	openInNewTab?: boolean
	selected?: boolean
	indicatorPosition?: ButtonIndicatorPosition
	elevation?: 1 | 2 | 3 | 4 | 5
	classList?: string[]
}

const {
	openInNewTab,
	indicatorPosition = ButtonIndicatorPosition.bottom,
	variant = ButtonVariant.transparent,
	focus,
	compact,
	selected,
	elevation,
	classList = [],
	...other
} = Astro.props

---

<a
	class:list={['c-btn', {
		'c-filled-btn': variant == ButtonVariant.filled,
		'c-tonal-btn': variant == ButtonVariant.tonal,
		'c-outlined-btn': variant == ButtonVariant.outlined
	}, ...classList]}
	data-c-indicator={selected? indicatorPosition : undefined}
	data-c-selected={setAttrIfExist(selected)}
	data-c-elevation={setAttrIfExist(elevation, true)}
	data-c-focus={setAttrIfExist(focus)}
	data-c-compact={setAttrIfExist(compact)}
	target={openInNewTab? '_blank' : undefined}
	rel={openInNewTab? 'noopener noreferrer' : undefined}
	{...other}>
	<slot/>
</a>