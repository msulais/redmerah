export enum Commands {
    /** @param {Pages} page `Pages` */
    change_page
}

export enum Pages {
    button,
    checkBox,
    colorPicker,
    datePicker,
    dateTimePicker,
    dialog,
    divider,
    drawer,
    dropdown,
    emoji,
    emojiPicker,
    expander,
    icon,
    list,
    menu,
    modal,
    popover,
    textField,
    toast,
    tooltip,
    switch
}