---
import CheckBox from '@/native-components/CheckBox/index.astro'
import TextField from '@/native-components/TextField/index.astro'
import Popover from '@/native-components/Popover/index.astro'
import Button from '@/native-components/Button/index.astro'
import Select from '@/native-components/Select/index.astro'
import SelectOption from '@/native-components/Select/SelectOption.astro'
import { ElementIds, ELEMENT_ID_PREFIX } from '../../_scripts/_enums'
import CSS from '../../_index.module.scss'
import { ButtonVariant } from '@/native-components/Button'
import { FlyoutPosition } from '@/enums/position'
---
<div
	class={CSS.bodyPage}
	role="tabpanel"
	id={ELEMENT_ID_PREFIX + ElementIds.panelPopover}>
	<h2>Popover</h2>
	<p>A popover is a small, temporary window that appears when a user interacts with an element (e.g., hovers over a button). It provides additional information, options, or tools related to the element.</p>
	<div class={CSS.bodyPlayground}>
		<Button
			ButtonVariant={ButtonVariant.tonal}
			id={ELEMENT_ID_PREFIX + ElementIds.panelPopoverPreviewButtonOpen}
			aria-controls={ELEMENT_ID_PREFIX + ElementIds.panelPopoverPreviewPopover}
			aria-haspopup="true">
			Open popover
		</Button>
		<Popover
			aria-label="Popover"
			id={ELEMENT_ID_PREFIX + ElementIds.panelPopoverPreviewPopover}
			PopoverAnchorBy={ELEMENT_ID_PREFIX + ElementIds.panelPopoverPreviewButtonOpen}
			PopoverContentAttr={{
				style: "padding: 16px;width: 280px"
			}}
			PopoverPadding={16}
			PopoverGap={8}>
			<TextField
				TextFieldInputAttr={{
					placeholder: 'Feedback',
					style: 'width: 100%'
				}}
			/>
			<p style="padding:8px 0 16px 0">Consequat commodo sint incididunt nulla duis commodo elit enim aliquip ex occaecat eiusmod.</p>
			<Button
				id={ELEMENT_ID_PREFIX + ElementIds.panelPopoverPreviewButtonClose}
				aria-controls={ELEMENT_ID_PREFIX + ElementIds.panelPopoverPreviewPopover}
				ButtonVariant={ButtonVariant.filled}>
				Close popover
			</Button>
		</Popover>
	</div>
	<div class={CSS.bodyOptions} id={ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptions}>
		<label id={ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsPositionLabel}>Position:</label>
		<Select
			aria-labelledby={ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsPositionLabel}
			id={ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsPosition}>
			{[
				[FlyoutPosition.centerBottom, 'Center bottom'],
				[FlyoutPosition.centerBottomToLeft, 'Center bottom to left'],
				[FlyoutPosition.centerBottomToRight, 'Center bottom to right'],
				[FlyoutPosition.centerCenter, 'Center center'],
				[FlyoutPosition.centerCenterBottom, 'Center center bottom'],
				[FlyoutPosition.centerCenterLeft, 'Center center left'],
				[FlyoutPosition.centerCenterLeftBottom, 'Center center left bottom'],
				[FlyoutPosition.centerCenterLeftTop, 'Center center left top'],
				[FlyoutPosition.centerCenterRight, 'Center center right'],
				[FlyoutPosition.centerCenterRightBottom, 'Center center right bottom'],
				[FlyoutPosition.centerCenterRightTop, 'Center center right top'],
				[FlyoutPosition.centerCenterTop, 'Center center top'],
				[FlyoutPosition.centerTop, 'Center top'],
				[FlyoutPosition.centerTopToLeft, 'Center top to left'],
				[FlyoutPosition.centerTopToRight, 'Center top to right'],
				[FlyoutPosition.leftBottom, 'Left bottom'],
				[FlyoutPosition.leftCenter, 'Left center'],
				[FlyoutPosition.leftCenterToBottom, 'Left center to bottom'],
				[FlyoutPosition.leftCenterToTop, 'Left center to top'],
				[FlyoutPosition.leftTop, 'Left top'],
				[FlyoutPosition.rightBottom, 'Right bottom'],
				[FlyoutPosition.rightCenter, 'Right center'],
				[FlyoutPosition.rightCenterToBottom, 'Right center to bottom'],
				[FlyoutPosition.rightCenterToTop, 'Right center to top'],
				[FlyoutPosition.rightTop, 'Right top'],
			].map(v =>
				<SelectOption value={v[0]} SelectOptionSelected={v[0] === FlyoutPosition.centerBottom}>{v[1]}</SelectOption>
			)}
		</Select>
		<label
			style="margin-left:12px"
			for={ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsPadding}>
			Padding:
		</label>
		<TextField
			TextFieldInputAttr={{
				value: '16',
				type: 'number',
				min: 0,
				style: 'width:100px;padding-right:4px',
				id: ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsPadding
			}}
		/>
		<label
			style="margin-left:12px"
			for={ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsGap}>
			Gap:
		</label>
		<TextField
			TextFieldInputAttr={{
				value: '8',
				type: 'number',
				min: 0,
				style: 'width:100px;padding-right:4px',
				id: ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsGap
			}}
		/>
		<CheckBox
			style={"margin-left:8px"}
			CheckBoxInputAttr={{
				id: ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsAnchor,
				checked: true
			}}>
			Anchor
		</CheckBox>
		<CheckBox
			CheckBoxInputAttr={{
				id: ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsAutofocus,
				checked: true
			}}>
			Autofocus
		</CheckBox>
		<CheckBox
			CheckBoxInputAttr={{
				id: ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsImportant,
			}}>
			Important
		</CheckBox>
		<CheckBox
			CheckBoxInputAttr={{
				id: ELEMENT_ID_PREFIX + ElementIds.panelPopoverOptionsDraggable,
			}}>
			Draggable
		</CheckBox>
	</div>
</div>