---
import Tooltip from '@/native-components/Tooltip/index.astro'
import Button from '@/native-components/Button/index.astro'
import TextField from '@/native-components/TextField/index.astro'
import TextFieldButton from '@/native-components/TextField/TextFieldButton.astro'
import Icon from '@/native-components/Icon/index.astro'
import Drawer from '@/native-components/Drawer/index.astro'
import DrawerButton from '@/native-components/Drawer/DrawerButton.astro'
import { ElementIds } from '../../_scripts/_enums'
import CSS from '../../_index.module.scss'
import { ButtonVariant } from '@/native-components/Button'
import { IconCodes } from '@/enums/icons'
---
<div
	class={CSS.bodyPage}
	role="tabpanel"
	id={ElementIds.panelDrawer}
	hidden>
	<h2>Drawer</h2>
	<p>A drawer is a navigation pattern that slides in from the edge of the screen, typically revealing a list of options or actions. It's often used to conserve screen space and provide access to secondary functions.</p>
	<div class={CSS.bodyPlayground}>
		<Tooltip>
			<Button
				popovertarget={ElementIds.panelDrawerPreviewDrawer}
				ButtonVariant={ButtonVariant.tonal}>
				Open drawer
			</Button>
		</Tooltip>
		<Drawer id={ElementIds.panelDrawerPreviewDrawer}>
			<Fragment slot="header">
				<Button
					style="width:min-content"
					popovertarget={ElementIds.panelDrawerPreviewDrawer}
					popovertargetaction='hide'
					aria-label="Close drawer">
					<Icon IconCode={IconCodes.dismiss}/>
				</Button>
				<TextField
					style="width:calc(100% - 24px);margin:8px 12px;padding-right: 4px"
					TextFieldInputAttr={{
						placeholder: 'Search',
						style: 'width:100%',
						autofocus: true
					}}>
					<TextFieldButton slot='trailing'>
						<Icon IconCode={IconCodes.search}/>
					</TextFieldButton>
				</TextField>
			</Fragment>
			<DrawerButton
				DrawerButtonSelected
				ButtonVariant={ButtonVariant.tonal}>
				<Icon IconCode={IconCodes.home}/>
				Home
			</DrawerButton>
			<DrawerButton>
				<Icon IconCode={IconCodes.info}/>
				About
			</DrawerButton>
			<DrawerButton>
				<Icon IconCode={IconCodes.apps}/>
				Apps
			</DrawerButton>
			<DrawerButton>
				<Icon IconCode={IconCodes.call}/>
				Contact
			</DrawerButton>
			<Fragment slot='footer'>
				<DrawerButton>
					<Icon IconCode={IconCodes.person}/>
					Account
				</DrawerButton>
				<DrawerButton>
					<Icon IconCode={IconCodes.settings}/>
					Settings
				</DrawerButton>
			</Fragment>
		</Drawer>
	</div>
	<div class={CSS.bodyOptions} id={ElementIds.panelDrawerOptions}>
	</div>
</div>