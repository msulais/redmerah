export enum Commands {
	// grad = gradient
	grad_edit = 'grad:edit',
	grad_use = 'grad:use',
	grad_openActions = 'grad:open-actions',

	// gradStop = gradient stop
	gradStop_pickColor = 'grad-stop:pick-color',
	gradStop_actions = 'grad-stop:actions'
}