export const ID = 'APP-'

export enum ElementIds {
	appbar,
	appbarInfoButton,
	appbarInfoMenu,
	appbarInfoMenuShareButton,
	appbarSettingsButton,
	appbarSettingsMenu,
	appbarSettingsAnimationMenu,
	appbarSettingsThemeMenu,
	bodyLevelText,
	bodyStatusIcon,
	bodyStatusText,
	dialogBrowserNotSupported,
}

export enum Links {
	broswerCompatibility = 'https://developer.mozilla.org/en-US/docs/Web/API/BatteryManager#browser_compatibility'
}

export enum RadioGroupNames {
	settingsAnimation = 'settings:animation',
	settingsTheme = 'settings:theme',
}