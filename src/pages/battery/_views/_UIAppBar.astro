---
import AppBar from '@/native-components/AppBar/index.astro'
import IconButton from '@/native-components/Button/IconButton.astro'
import Tooltip from '@/native-components/Tooltip/index.astro'
import { APP_BATTERY as APP } from '@/constants/apps'
import { ElementIds } from '../_scripts/_enums'
import { IconCodes } from '@/enums/icons'
---

<AppBar id={ElementIds.appbar}>
	<Fragment slot="leading">
		<img
			src={APP.logoUrl}
			alt={APP.name + ' logo'}
			width={32}
			style="margin: 0 8px"
		/>
	</Fragment>
	<Fragment slot="headline">{APP.name}</Fragment>
	<Tooltip slot="trailing">
		<IconButton
			id={ElementIds.appbarInfoButton}
			data-tooltip="Info"
			IconButtonCode={IconCodes.info}
			aria-label="Info"
			popovertarget={ElementIds.appbarInfoMenu}
		/>
		<IconButton
			id={ElementIds.appbarSettingsButton}
			data-tooltip="Settings"
			IconButtonCode={IconCodes.settings}
			aria-label="Settings"
			popovertarget={ElementIds.appbarSettingsMenu}
		/>
	</Tooltip>
</AppBar>