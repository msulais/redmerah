---
import SideBar from '@/components/SideBar/index.astro'
import Icon from '@/components/Icon/index.astro'
import IconButton from '@/components/Button/IconButton.astro'
import Drawer from '@/components/Drawer/index.astro'
import DrawerButton from '@/components/Drawer/DrawerButton.astro'
import Button from '@/components/Button/index.astro'
import SideBarButton from '@/components/SideBar/SideBarButton.astro'
import { CButton } from '@/components/Button'
import { IconCodes } from '@/enums/icons'
import { CSSClasses } from '../_styles/_css'
import { Pages } from '../_scripts/_shared/_enums'
import { ElementIds } from '../_scripts/_shared/_ids'
import { DEFAULT_PAGE } from '../_scripts/_shared/_constant'
import { pxToRem } from '@/utils/css'

const items: {
	icon: number
	name: string
	page: Pages
	controls: string
}[] = [
	{
		icon: IconCodes.calculator,
		name: 'Basic',
		page: Pages.basic,
		controls: ElementIds.pg_basic,
	},
	{
		icon: IconCodes.beaker,
		name: 'Scientific',
		page: Pages.scientific,
		controls: ElementIds.pg_scientific,
	},
	{
		icon: IconCodes.arrowSort,
		name: 'Converter',
		page: Pages.converter,
		controls: ElementIds.pg_converter,
	},
	{
		icon: IconCodes.code,
		name: 'Programmer',
		page: Pages.programmer,
		controls: ElementIds.pg_programmer,
	},
	{
		icon: IconCodes.calendar,
		name: 'Date',
		page: Pages.date,
		controls: ElementIds.pg_date,
	},
]
---

<SideBar
	role="tablist"
	class={CSSClasses.nav_sideBar}
	id={ElementIds.nav_sideBar}
	aria-label="Navigation">
	<IconButton
		style={{width: 'fit-content'}}
		IconButtonCode={IconCodes.lineHorizontal3}
		data-tooltip="Minimize side bar"
		id={ElementIds.nav_minimizeBtn}
	/>
	{items.map(v => <SideBarButton
		role="tab"
		style={{
			'padding-left': pxToRem(10) + 'rem'
		}}
		data-page={v.page}
		aria-selected={DEFAULT_PAGE === v.page}
		aria-controls={v.controls}
		SideBarButtonSelected={DEFAULT_PAGE === v.page}
		ButtonVariant={DEFAULT_PAGE === v.page ? CButton.Variant.tonal : undefined}>
		<Icon
			slot="leading"
			IconCode={v.icon}
			data-tooltip={v.name}
		/>
		{v.name}
	</SideBarButton>)}
</SideBar>
<Drawer
	id={ElementIds.nav_drawer}
	aria-label="Navigation">
	<Button
		slot="header"
		style="width:min-content"
		popovertarget={ElementIds.nav_drawer}
		popovertargetaction="hide">
		<Icon IconCode={IconCodes.lineHorizontal3}/>
	</Button>
	{items.map(v => <DrawerButton
		aria-selected={DEFAULT_PAGE === v.page}
		aria-controls={v.controls}
		data-page={v.page}
		DrawerButtonSelected={DEFAULT_PAGE === v.page}
		ButtonVariant={DEFAULT_PAGE === v.page ? CButton.Variant.tonal : undefined}>
		<Icon
			IconCode={v.icon}
		/>
		{v.name}
	</DrawerButton>)}
</Drawer>