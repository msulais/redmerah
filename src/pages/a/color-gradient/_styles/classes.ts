import CSS from './styles.module.scss'

export class CSSClasses {
	static readonly body = CSS.body
	static readonly bodyGradients = CSS.bodyGradients
	static readonly bodyProperties = CSS.bodyProperties
	static readonly bodySavedGradient = CSS.bodySavedGradient
	static readonly bodyPreview = CSS.bodyPreview
	static readonly bodyEditor = CSS.bodyEditor
	static readonly bodyGradientControl = CSS.bodyGradientControl
	static readonly bodyPreviewContainer = CSS.bodyPreviewContainer
}

export class CSSGroup {
	static readonly stopSizeInput = 'stop-size-input'
	static readonly stopColorInput = 'stop-color-input'
}