---
import * as Icons from '@/enums/icons.enum.js'
import * as Ids from '../_scripts/shared/ids.enum.js'
import * as Pages from '../_scripts/shared/pages.enum.js'
---
<br-navigation slot="left-sidebar" id={Ids.Navigation}>
	<br-focusgroup br:direction="vertical">
		<button
			aria-label="Toggle navigation"
			br:tooltip="Toggle navigation"
			br:command-for={Ids.Navigation}>
			<br-icon set:html={Icons.LineHorizontal3}/>
		</button>
		<a
			br:as="navigationitem"
			href={`?page=${Pages.Length}`}
			br:query={`page=${Pages.Length}|none`}
			br:label="Length">
			<br-icon set:html={Icons.Ruler}/>
		</a>
		<a
			br:as="navigationitem"
			href={`?page=${Pages.Angle}`}
			br:query={`page=${Pages.Angle}`}
			br:label="Angle">
			<br-icon set:html={Icons.Remote}/>
		</a>
		<a
			br:as="navigationitem"
			href={`?page=${Pages.Time}`}
			br:query={`page=${Pages.Time}`}
			br:label="Time">
			<br-icon set:html={Icons.Timer}/>
		</a>
	</br-focusgroup>
</br-navigation>