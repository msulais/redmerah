import CSS from './_styles.module.scss'

export class CSSClasses {
	static readonly body = CSS.body
	static readonly bodyPicker = CSS.bodyPicker
	static readonly bodyInputs = CSS.bodyInputs
	static readonly pickerRect = CSS.pickerRect
	static readonly pickerImageRect = CSS.pickerImageRect
	static readonly pickerSlider = CSS.pickerSlider
}